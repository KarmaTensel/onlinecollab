<nav class="navbar navbar-expand-lg navbar-light bg-light">
  
  <div class="container">
      
      <ul class="navbar-nav">
        
        <li class="nav-item">
          <strong><%= link_to "AskAway", root_path, class:"navbar-brand" %></strong>
        </li>
        
        <li class="nav-item">
          <strong><%= link_to "Blogs", posts_path, class:"navbar-brand" %></strong>
        </li>

        <% if user_signed_in? %>
          <% if current_user.role == 'admin' %>

            <li class="nav-item">
              <strong><%= link_to "Users", users_path, class:"navbar-brand" %></strong>
            </li>

          <% end %>
        <% end %>

        <li class="nav-item">
          <strong><%= link_to "Guide & Instructions", root_path, class:"navbar-brand" %></strong>
        </li>

      </ul>

    <div class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            More
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to "User Profile", current_user, class:"dropdown-item" %></strong></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class:"dropdown-item" %></li>
          </ul> 
        </div>
      <% end %>
    </div>

  </div>

</nav>