<%= form_with model: [ @post, @post.comments.build ] do |f| %>
    <%= f.rich_text_area :body, class: "form-control", placeholder: "Say something" %>
    <br>
    <div class="actions">
        <%= f.submit class:"btn btn-success"%>
    </div>
<% end %>
