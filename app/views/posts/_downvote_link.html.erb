<% if current_user %>
  <%# <% vote = current_user&.voted_for?(post) ? "Down" : "Up" %>
  <%= link_to downvote_post_path(post), method: :put, remote: :true, class: "link" do %>
    <button class="btn btn-secondary"><i class="bi bi-chevron-down"></i></button>
    <h4 class="lff-pad"> &nbsp;<%= @post.get_downvotes.size %></h4>
  <% end %>
<% end %>